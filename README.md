# ViT from Scratch

A learning repo for building a Vision Transformer piece by piece.

Current focus:

- tiny `Module` abstraction
- neuron and layer building blocks
- MLP block
- LayerNorm
- image patch embeddings
- PyTorch-compatible versions for comparison

Code lives under:

```text
learning/cifar10_vit/
```
