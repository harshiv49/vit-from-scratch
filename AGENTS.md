# Agent Rules

- Preserve user-written comments. Do not delete, rewrite, or simplify them unless the user explicitly asks.
- When changing code around an existing comment, keep the original comment and add new clarification below it if needed.
- If a comment becomes technically wrong because of a code change, update it minimally and call out that change.
